#include <bits/stdc++.h>

using namespace std;

int main(int argc, char const *argv[]) {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(0);

    int v, t;
    while(cin >> v >> t) {
        cout << 2*v*t << endl;
    }

    return 0;
}